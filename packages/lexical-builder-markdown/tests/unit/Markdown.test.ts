import {} from "@etrepum/lexical-builder-markdown";
import { describe, it, expect } from "vitest";

describe("Markdown", () => {
  it("should have tests", () => {
    expect(true).toBe(true);
  });
});
